---
name: TypeScript Guidelines
description: Type safety, strict typing, no `any`, generics, type guards for the StackCraft app
globs:
  - '**/*.ts'
  - '**/*.tsx'
alwaysApply: true
---

# TypeScript Guidelines

## Public API

- All exports have explicit types (params + return).
- Generic params have constraints + descriptive names (`TUser`, `TResult` — not `T`, `R`).
- Prefer `type` over `interface`. No `I` prefix, no `Type` suffix.

```typescript
// ✅
export const ProcessManager = defineService({
  name: 'app/ProcessManager',
  lifetime: 'singleton',
  factory: (): ProcessManager => ({ /* ... */ }),
})

// ❌ implicit return type, single-letter generic
export function fetchData<T>(url) { return fetch(url) }
```

## NO `any` — one exception

`any` forbidden in values, params, return types. **Exception:** generic constraint position when variance breaks `unknown` inference (e.g. callback param positions).

```typescript
// ✅ unknown for truly unknown
const process = (data: unknown): string =>
  typeof data === 'string' ? data : JSON.stringify(data)

// ✅ any only allowed in constraint position
type ExtractPaths<T extends Record<string, NestedRoute<any>>> = { /* ... */ }

// ❌ any anywhere else
export function process(data: any): string { return data.toString() }
```

Alternatives: `unknown` + type guard, generics, union types, utility types.

## Inference

Let TS infer internally. Be explicit at API boundaries, complex switch/case, or when inference is ambiguous.

```typescript
// ✅ inferred internally
const services = ['gateway', 'web']
const count = services.length

// ✅ explicit at exports
export const getServiceById = async (id: string): Promise<Service | null> => { /* ... */ }

// ❌ noisy
const count: number = 5
```

## API contract types

The frontend and service share contracts through `common/src/apis/<module>.ts`. Each module exports a `RestApi`-derived interface (e.g. `StacksApi`, `ServicesApi`, `IdentityApi`). Always import the typed module API from `common` so calls are type-checked end-to-end.

```typescript
import type { ServicesApi } from 'common'

// Service side
useRestService<ServicesApi>({ injector, api: { /* ... */ } })

// Frontend side (auto-typed via the api client)
const { result } = await apiClient.call({ method: 'GET', action: '/services/:id', url: { id } })
```

## Type guards

Use guards for runtime checks. Use them, not type assertions.

```typescript
// ✅
export const isService = (value: unknown): value is Service =>
  typeof value === 'object' && value !== null && 'id' in value

const isNonNullable = <T>(value: T | null | undefined): value is T =>
  value !== null && value !== undefined

services.filter(isNonNullable) // type-narrowed
```

## Discriminated unions

For multi-state values. Always include a `status` / `kind` discriminator.

```typescript
// ✅
type ServiceState =
  | { status: 'idle' }
  | { status: 'starting' }
  | { status: 'running'; pid: number }
  | { status: 'failed'; error: string }

// ❌ no discriminator → cannot narrow
type Bad = { running: boolean; error?: string; pid?: number }
```

## Observable typing

`ObservableValue<T>` is invariant — provide explicit `T`, especially for nullable / union states.

```typescript
// ✅
public currentService = new ObservableValue<Service | null>(null)
public state = new ObservableValue<ServiceState>({ status: 'idle' })

// ❌ inference widens to ObservableValue<null>
public currentService = new ObservableValue(null)
```

## Assertions

Avoid. Use type guards or `as const` instead.

```typescript
// ✅ as const for literal types
const colors = ['red', 'green', 'blue'] as const
type Color = (typeof colors)[number]

// ✅ assertion only when DOM API leaves no choice
const el = document.getElementById('x') as HTMLInputElement | null

// ❌ dangerous
const user = unknownValue as User       // prefer guard
const x = maybe!                        // prefer optional chaining
```

Avoid `!` non-null assertion. Prefer `?.` and `??`.

## Utility types

Reuse built-ins (`Partial`, `Pick`, `Omit`, `Record`, `Required`, `Readonly`). Export project-specific helpers when reused 2+ times.

## Double-cast anti-pattern

Never `as unknown as T`. If you need a cast that strong, you're missing a type guard or a generic. Refactor.

## Checklist (public API)

- [ ] No `any` (except generic constraint exception)
- [ ] Explicit types on all exports
- [ ] Generic params named & constrained
- [ ] Type guards for runtime checks
- [ ] Discriminated unions for multi-state
- [ ] No `as unknown as T`, no `!` non-null
- [ ] `ObservableValue` types explicit

Tools: `yarn build`, strict mode is on.
